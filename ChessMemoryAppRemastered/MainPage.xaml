<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ChessMemoryAppRemastered.MainPage"
             BackgroundColor="{StaticResource Black}"
             Shell.BackgroundColor="{StaticResource Black}"
             Shell.TitleColor="White"
             Shell.TabBarBackgroundColor="{StaticResource Black}"
             >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*"/>
            <ColumnDefinition Width="0.6*"/>
            <ColumnDefinition Width="0.2*"/>
        </Grid.ColumnDefinitions>

        <VerticalStackLayout Grid.Row="0"
                             Grid.Column="0">
            <Button x:Name="btnAddMove"
                    Text="Add Move"/>
            <Button x:Name="btnNextMove"
                    Text="Next Move"/>
            <Button x:Name="btnPreviousMove"
                    Text="Previous Move"/>
            <Button x:Name="btnFirstMove"
                    Text="First Move"/>
        </VerticalStackLayout>
        
        <AbsoluteLayout x:Name="absoluteLayoutChessBoard"
                        Grid.Row="0"
                        Grid.Column="1"
                        BackgroundColor="Red"/>
    </Grid>

</ContentPage>
